// Chapter 7: Exceptions & I/O — Top Questions, Quiz, Short Notes

export const topQuestions = [
  { id: 1, difficulty: 'easy', q: 'What is the difference between checked and unchecked exceptions?', a: 'Checked: subclasses of Exception (not RuntimeException) — compiler requires catch or throws declaration. Examples: IOException, SQLException. Unchecked: RuntimeException subclasses — no compiler enforcement. Examples: NPE, IllegalArgument. Errors (OutOfMemory) are unchecked and usually fatal.' },
  { id: 2, difficulty: 'medium', q: 'What is try-with-resources?', a: 'Automatically closes resources implementing AutoCloseable at block end, even on exception. `try (var is = new FileInputStream(f)) {...}`. Multiple resources closed in reverse order. Exceptions from close() are suppressed if a primary exception already propagated.' },
  { id: 3, difficulty: 'medium', q: 'What is exception chaining?', a: 'Wrapping the original exception in a new one while preserving cause. `throw new ServiceException("message", cause)`. Access via getCause(). Always chain when translating exceptions at layer boundaries. Losing the cause makes debugging very hard.' },
  { id: 4, difficulty: 'hard', q: 'Can a finally block suppress an exception?', a: 'Yes — if finally has a return statement or throws an exception itself, it suppresses the original exception. This silently loses the original error. Try-with-resources avoids this: close() exceptions become suppressed exceptions accessible via getSuppressed(), not lost.' },
  { id: 5, difficulty: 'medium', q: 'What is multi-catch (Java 7+)?', a: '`catch (IOException | SQLException e)` catches multiple exception types in one block. e is effectively final (cannot be reassigned). Cleaner than duplicate catch blocks. The types must not have a subtype relationship with each other.' },
  { id: 6, difficulty: 'medium', q: 'What is the difference between throws and throw?', a: '`throws` in method signature declares that the method may throw a checked exception. `throw` (statement) actually throws an exception object. `public void read() throws IOException { throw new IOException(); }` — throws declares, throw executes.' },
  { id: 7, difficulty: 'hard', q: 'What are suppressed exceptions?', a: 'When try-with-resources: if primary exception AND close() throws, close exception is suppressed and attached to primary. Access via `e.getSuppressed()`. Manually add: `primaryEx.addSuppressed(ex)`. Preserves full diagnostic info vs finally block which loses the original.' },
  { id: 8, difficulty: 'medium', q: 'What is the difference between InputStream/OutputStream and Reader/Writer?', a: 'InputStream/OutputStream: raw bytes — for binary data (images, network). Reader/Writer: characters with charset — for text. InputStreamReader/OutputStreamWriter bridge between the two. ALWAYS specify charset explicitly: `new InputStreamReader(is, StandardCharsets.UTF_8)`.' },
  { id: 9, difficulty: 'medium', q: 'What is the NIO.2 Path API?', a: 'java.nio.file.Path (Java 7+): immutable path, works with Files utility. Better than File: proper exception handling (not boolean returns), symbolic link support, file attributes, directory streams, WatchService. `Path.of("a/b/c")` (Java 11+) or `Paths.get("a/b")`.' },
  { id: 10, difficulty: 'medium', q: 'What are the common Files utility methods?', a: 'Files.readAllBytes(path), Files.readString(path) (Java 11+), Files.lines(path) (lazy Stream<String>), Files.write(path, bytes), Files.writeString(path, str) (Java 11+), Files.copy/move/delete, Files.exists/isDirectory/isRegularFile, Files.walk(path) (recursive), Files.createDirectories(path).' },
  { id: 11, difficulty: 'easy', q: 'What is serialization and what are its security risks?', a: 'Serialization converts object state to byte stream (Serializable interface). Risks: deserialization of untrusted data is a major RCE vulnerability — attackers can craft malicious byte streams. Mark sensitive fields transient. Use JSON/XML instead of Java serialization for external data. serialVersionUID controls version compatibility.' },
  { id: 12, difficulty: 'medium', q: 'When should you create a custom exception?', a: 'Create custom exception when: (1) you need domain-specific context, (2) callers need to handle it differently from existing exceptions, (3) you want to establish a hierarchy for your API. Extend RuntimeException for unchecked, Exception for checked. Include constructor with message and cause.' },
  { id: 13, difficulty: 'hard', q: 'What is the difference between BufferedReader and Scanner for file reading?', a: 'BufferedReader: buffered, efficient, readLine() — use for large files. Scanner: tokenized, uses regex, convenient for structured input, much slower. Files.lines(path) returns lazy Stream<String> — best for functional processing. Files.readString() for small text files.' },
  { id: 14, difficulty: 'medium', q: 'What is the WatchService in NIO.2?', a: 'WatchService monitors directory for changes: CREATE, MODIFY, DELETE events. Register a directory with watchService.register(dir, ENTRY_CREATE). Poll with watchService.poll() or take() (blocking). Used for hot-reload, file sync applications. Only watches one level — use Files.walk for recursive.' },
  { id: 15, difficulty: 'medium', q: 'What is ObjectOutputStream and when should you avoid it?', a: 'ObjectOutputStream serializes objects to a stream. readObject() deserializes. Security: NEVER deserialize from untrusted sources (Java deserialization vulnerabilities). Alternatives: JSON (Jackson/Gson), Protocol Buffers, Avro, XML. Use whitelist deserialization filters (Java 9+) if you must use Java serialization.' },
  { id: 16, difficulty: 'easy', q: 'What is the finally block?', a: 'Executes after try/catch regardless of exception or normal return. Use for cleanup (non-AutoCloseable resources). Does NOT run if JVM exits (System.exit(), JVM crash, infinite loop). Return in finally overrides return in try/catch (avoid this). Prefer try-with-resources for resource cleanup.' },
  { id: 17, difficulty: 'hard', q: 'What is re-throwing an exception?', a: 'Catching an exception and throwing it again. (1) `throw e;` — rethrow original (checked/unchecked). (2) `throw new WrapperEx("msg", e);` — wrap with cause (recommended). (3) Java 7+ precise rethrow: if catch(Exception e) rethrows, compiler knows only the declared type was thrown. Avoid catch-and-ignore.' },
  { id: 18, difficulty: 'medium', q: 'What is the difference between close() and flush() in I/O?', a: '`flush()`: forces buffered data to be written to the underlying stream immediately without closing. `close()`: flushes and releases resources. A buffered writer might hold data in memory — without flush/close, data may be lost. Try-with-resources auto-calls close().' },
  { id: 19, difficulty: 'medium', q: 'What are common IOException subclasses?', a: 'FileNotFoundException, EOFException (end of stream), SocketException (network), ConnectException, MalformedURLException, FileAlreadyExistsException (NIO.2), AccessDeniedException (NIO.2), NoSuchFileException (NIO.2). NIO.2 exceptions extend IOException and are more descriptive than old File API.' },
  { id: 20, difficulty: 'hard', q: 'What is a FileChannel and when would you use it?', a: 'FileChannel (NIO): non-blocking I/O, memory-mapped files (FileChannel.map()), transferTo/transferFrom for efficient OS-level copy, file locking. Faster than streams for large files. Use with ByteBuffer. `FileChannel fc = FileChannel.open(path, READ, WRITE)`. For most apps, Files utility methods are sufficient.' },
]

export const quiz = [
  { id: 1, q: 'Which is a checked exception?', options: ['NullPointerException', 'IOException', 'ArrayIndexOutOfBoundsException', 'ClassCastException'], answer: 1, explanation: 'IOException is checked — compiler requires you to catch or declare it. The others are RuntimeException (unchecked).' },
  { id: 2, q: 'What interface must a class implement for try-with-resources?', options: ['Closeable', 'AutoCloseable', 'Either Closeable or AutoCloseable', 'Serializable'], answer: 2, explanation: 'AutoCloseable is the minimum requirement. Closeable extends AutoCloseable. Both work in try-with-resources.' },
  { id: 3, q: 'What is the difference between `throw` and `throws`?', options: ['No difference', 'throw executes; throws declares in method signature', 'throws executes; throw declares', 'throw for checked, throws for unchecked'], answer: 1, explanation: '`throw` (statement) actually throws the exception. `throws` in method signature declares that the method may throw a checked exception.' },
  { id: 4, q: 'What happens to suppressed exceptions in try-with-resources?', options: ['They are lost', 'They cause a second exception', 'They are attached to the primary exception', 'They are printed to stderr'], answer: 2, explanation: 'Suppressed exceptions are attached to the primary exception. Access with e.getSuppressed(). They are not lost.' },
  { id: 5, q: 'Which class should you use for efficient text file reading?', options: ['FileReader', 'Scanner', 'BufferedReader', 'InputStreamReader'], answer: 2, explanation: 'BufferedReader buffers reads for efficiency. FileReader is unbuffered. Scanner is slower due to regex tokenization.' },
  { id: 6, q: 'What does `final` mean for exception variable in multi-catch?', options: ['Cannot catch that exception type again', 'The variable is effectively final (cannot reassign)', 'The exception becomes unchecked', 'The exception cannot be wrapped'], answer: 1, explanation: 'In multi-catch (IOException | SQLException e), e is effectively final — you cannot reassign it to a different exception.' },
  { id: 7, q: 'What is the NIO.2 replacement for java.io.File?', options: ['java.nio.File', 'java.nio.file.Path', 'java.nio.FilePath', 'java.io.Path'], answer: 1, explanation: 'java.nio.file.Path (Java 7+) is the modern replacement for java.io.File, used with the Files utility class.' },
  { id: 8, q: 'Which Files method reads an entire text file as a String (Java 11+)?', options: ['Files.readAllBytes()', 'Files.readText()', 'Files.readString()', 'Files.toString()'], answer: 2, explanation: 'Files.readString(path) reads the file as a String using UTF-8 by default. Added in Java 11.' },
  { id: 9, q: 'What is the security risk of Java serialization?', options: ['Memory leaks', 'Remote code execution via malicious deserialization', 'Version incompatibility only', 'Performance only'], answer: 1, explanation: 'Deserializing untrusted data is a critical security vulnerability — attackers can execute arbitrary code. Prefer JSON for external data.' },
  { id: 10, q: 'When is finally NOT executed?', options: ['When an exception is thrown', 'When a return is in try', 'When System.exit() is called', 'When catch handles the exception'], answer: 2, explanation: 'System.exit() terminates the JVM — finally is NOT called. Also: infinite loop, JVM crash. In all other cases, finally runs.' },
  { id: 11, q: 'What marks a field to be excluded from serialization?', options: ['static', 'final', 'volatile', 'transient'], answer: 3, explanation: 'transient fields are excluded from Java serialization. static fields are also not serialized (not instance state).' },
  { id: 12, q: 'What does Files.lines(path) return?', options: ['String[]', 'List<String>', 'Stream<String>', 'BufferedReader'], answer: 2, explanation: 'Files.lines(path) returns a lazy Stream<String> — lines are read on demand. Must be closed (use try-with-resources).' },
  { id: 13, q: 'What is the purpose of serialVersionUID?', options: ['Encrypts the serialized form', 'Controls version compatibility for deserialization', 'Sets the buffer size', 'Defines the file format'], answer: 1, explanation: 'serialVersionUID is checked during deserialization. If it does not match, InvalidClassException is thrown. Explicitly declare it to avoid accidental incompatibilities.' },
  { id: 14, q: 'Which exception is thrown when a file is not found?', options: ['IOException', 'FileNotFoundException', 'NoSuchFileException', 'Both B and C (in different APIs)'], answer: 3, explanation: 'FileNotFoundException (java.io) and NoSuchFileException (java.nio.file) both represent missing files, depending on which API you use.' },
  { id: 15, q: 'How do you close multiple resources in try-with-resources?', options: ['Separate try blocks', 'Comma-separated in one try', 'Call close() manually', 'Semicolon-separated in one try'], answer: 3, explanation: 'Multiple resources are semicolon-separated: `try (Resource1 r1 = ...; Resource2 r2 = ...)`. Closed in reverse order (r2 first).' },
]

export const shortNotes = {
  title: 'Exceptions & I/O — Quick Reference',
  color: 'cyan',
  keyPoints: [
    { icon: '⚠️', title: 'Checked vs Unchecked', text: 'Checked (IOException): compiler enforces. Unchecked (RuntimeException): no enforcement. Errors: fatal JVM issues.' },
    { icon: '🔒', title: 'Try-with-Resources', text: 'Auto-closes AutoCloseable resources. Multiple resources: semicolon-separated, closed in reverse order. Suppresses close() exceptions.' },
    { icon: '🔗', title: 'Exception Chaining', text: 'Always pass the cause: `new ServiceEx("msg", cause)`. getCause() retrieves it. Never swallow exceptions silently.' },
    { icon: '📂', title: 'NIO.2 Files API', text: 'Path.of(), Files.readString(), Files.writeString(), Files.lines() (lazy stream), Files.walk(). Better than legacy File.' },
    { icon: '🔄', title: 'Multi-catch (Java 7+)', text: '`catch (IOException | SQLException e)` — cleaner, e is effectively final. Types must not be subtypes of each other.' },
    { icon: '📡', title: 'Stream Types', text: 'InputStream/OutputStream: bytes (binary). Reader/Writer: chars (text). Always specify charset: StandardCharsets.UTF_8.' },
    { icon: '🛡️', title: 'Serialization Warning', text: 'Java deserialization = security risk. Use JSON (Jackson/Gson) for external data. transient excludes fields.' },
    { icon: '🔄', title: 'finally Guarantee', text: 'finally always runs except: System.exit(), JVM crash, infinite loop. A return in finally overrides try/catch return.' },
  ],
  quickFacts: [
    'try-with-resources was added in Java 7',
    'Multi-catch (IOException | SQLException) added in Java 7',
    'Files.readString/writeString added in Java 11',
    'NIO.2 (java.nio.file) added in Java 7',
    'Suppressed exceptions: e.getSuppressed() returns an array',
    'Standard charsets: StandardCharsets.UTF_8, ISO_8859_1, US_ASCII',
    'WatchService monitors directory changes (NIO.2)',
  ],
  rememberThis: [
    'Always chain exceptions — pass the cause',
    'try-with-resources is always better than try/finally for resources',
    'Never serialize untrusted external data in Java',
    'Close() exceptions are suppressed, not lost, in try-with-resources',
  ],
}
