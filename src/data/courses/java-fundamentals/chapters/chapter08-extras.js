// Chapter 8: Functional Programming & Streams — Top Questions, Quiz, Short Notes

export const topQuestions = [
  { id: 1, difficulty: 'medium', q: 'What is a functional interface?', a: 'An interface with exactly one abstract method (SAM). @FunctionalInterface annotation enforces this at compile time. Built-ins: Function<T,R>, Consumer<T>, Supplier<T>, Predicate<T>, BiFunction<T,U,R>, UnaryOperator<T>, BinaryOperator<T>, Runnable, Callable<V>.' },
  { id: 2, difficulty: 'medium', q: 'What is the difference between map() and flatMap() in streams?', a: 'map(): 1-to-1 transformation, each element → one element. flatMap(): 1-to-many, each element → Stream, then all streams flattened. Use flatMap for nested collections: `List<List<String>> → flatMap(List::stream) → Stream<String>`. Also in Optional: `Optional<Optional<T>> → flatMap → Optional<T>`.' },
  { id: 3, difficulty: 'medium', q: 'What is the difference between intermediate and terminal stream operations?', a: 'Intermediate: lazy, return a new Stream (filter, map, sorted, distinct, limit, skip, peek). Terminal: trigger processing, return result (collect, forEach, reduce, count, findFirst, anyMatch, allMatch, noneMatch, min, max). Streams are single-use — consumed after terminal operation.' },
  { id: 4, difficulty: 'hard', q: 'What is the difference between reduce() and collect()?', a: 'reduce(): immutable folding to a single value via BinaryOperator. collect(): mutable reduction to a container (List, Map, String) using a Collector. Use collect() for building collections. Use reduce() for scalar results (sum, product, concatenation). Collectors: toList, groupingBy, joining, counting, partitioningBy.' },
  { id: 5, difficulty: 'medium', q: 'What is Optional and how should it be used?', a: 'Optional<T>: a container that may or may not hold a value. Methods: isPresent/isEmpty, get (risky), orElse, orElseGet, orElseThrow, ifPresent, map, flatMap, filter, or (Java 9). Use as return type (not parameter/field). Never call get() without isPresent — use orElse/orElseThrow.' },
  { id: 6, difficulty: 'medium', q: 'What are the 4 types of method references?', a: '1. Static: `Integer::parseInt`. 2. Instance on specific object: `System.out::println`. 3. Instance on arbitrary object of type: `String::toUpperCase` (called on stream element). 4. Constructor: `ArrayList::new`. All are syntactic sugar for lambdas with matching functional interface signature.' },
  { id: 7, difficulty: 'hard', q: 'What are Collectors.groupingBy() and partitioningBy()?', a: '`groupingBy(classifier)` → `Map<K, List<T>>`. With downstream: `groupingBy(Person::getCity, counting())`. `partitioningBy(predicate)` → `Map<Boolean, List<T>>` (true/false groups). Both are powerful for data aggregation in one pass.' },
  { id: 8, difficulty: 'hard', q: 'What are parallel streams and when should you use them?', a: 'stream.parallel() splits work across ForkJoinPool.commonPool(). Benefits: CPU-bound tasks on large datasets. Pitfalls: (1) I/O-bound tasks (no benefit), (2) small collections (overhead > speedup), (3) shared mutable state (bugs), (4) ordered operations may be slower. Benchmark first (JMH).' },
  { id: 9, difficulty: 'medium', q: 'What is an effectively final variable?', a: 'A local variable that is never reassigned after initial assignment — it behaves like final even without the keyword. Lambdas and anonymous classes can only capture final or effectively final variables. The constraint exists because the lambda may outlive the enclosing scope.' },
  { id: 10, difficulty: 'medium', q: 'What is CompletableFuture?', a: 'Composable async programming. Key methods: supplyAsync(Supplier) — async value. thenApply(fn) — transform. thenAccept(consumer) — consume. thenCompose(fn) — chain futures. thenCombine(other, fn) — combine two. exceptionally(fn) — error handle. allOf/anyOf for multiple futures. Use over plain Future.' },
  { id: 11, difficulty: 'medium', q: 'What is the Stream.of() method?', a: '`Stream.of(a, b, c)` creates a sequential stream from explicit values. `Stream.empty()` creates an empty stream. `Stream.generate(supplier)` creates an infinite stream. `Stream.iterate(seed, fn)` creates an infinite sequence. IntStream.range/rangeClosed for int ranges.' },
  { id: 12, difficulty: 'hard', q: 'What is Collectors.joining()?', a: '`Collectors.joining()` concatenates stream strings. `joining(delimiter)` adds separator. `joining(delimiter, prefix, suffix)` wraps result. Example: `stream.collect(joining(", ", "[", "]"))` → "[a, b, c]". Very efficient — uses StringBuilder internally.' },
  { id: 13, difficulty: 'medium', q: 'What is a lambda closure?', a: 'A lambda that captures variables from its enclosing scope forms a closure. Captured variables must be final or effectively final. The lambda carries these values as part of its state. Instance fields and static fields can be freely captured and even mutated (though mutable state in lambdas is usually bad design).' },
  { id: 14, difficulty: 'hard', q: 'What is the difference between Stream.peek() and Stream.forEach()?', a: '`peek()`: intermediate operation, used for debugging/side effects during stream processing — does NOT trigger execution alone. `forEach()`: terminal operation, consumes the stream. Use peek() to inspect elements mid-pipeline; forEach() to produce side effects as the terminal step.' },
  { id: 15, difficulty: 'medium', q: 'What does Optional.orElseGet() vs orElse() differ?', a: '`orElse(value)`: always evaluates the default value, even if Optional is present. `orElseGet(supplier)`: lazily evaluates the supplier only if Optional is empty. Use orElseGet for expensive defaults to avoid unnecessary computation when the value is present.' },
  { id: 16, difficulty: 'hard', q: 'What is Collectors.toUnmodifiableList() vs toList() (Java 16+)?', a: '`Collectors.toList()`: returns a mutable List (usually ArrayList). `Collectors.toUnmodifiableList()`: returns immutable list. Java 16+: `Stream.toList()` — convenient shorthand for toUnmodifiableList() that is also null-rejecting. Prefer Stream.toList() for immutable results in Java 16+.' },
  { id: 17, difficulty: 'medium', q: 'What is Function composition in Java?', a: '`Function.compose(before)`: apply before first, then this. `Function.andThen(after)`: apply this first, then after. `Predicate.and/or/negate()` for predicate composition. Enables building pipelines without lambda nesting: `f.andThen(g)` reads left-to-right.' },
  { id: 18, difficulty: 'hard', q: 'What is the difference between sequential and parallel stream encounter order?', a: 'Ordered sources (List, arrays): sequential streams maintain encounter order. Parallel streams may process out of order for efficiency. Use `forEachOrdered()` instead of forEach() in parallel streams when order matters. sorted() on parallel stream adds synchronization overhead.' },
  { id: 19, difficulty: 'medium', q: 'What are primitive streams (IntStream, LongStream, DoubleStream)?', a: 'Specialized streams avoiding boxing overhead. IntStream.range(0,10), IntStream.of(1,2,3), Arrays.stream(intArr). Extra terminals: sum(), average(), min(), max(), summaryStatistics(). `boxed()` converts to Stream<Integer>. Use for performance-critical numeric processing.' },
  { id: 20, difficulty: 'hard', q: 'What is the Spliterator?', a: 'Spliterator is the underlying traversal mechanism for streams — split-able Iterator. Provides trySplit() (divide for parallel), tryAdvance() (visit next), estimateSize(). Custom Spliterators enable custom sources in streams. Usually you don\'t implement this directly — collections provide their own.' },
]

export const quiz = [
  { id: 1, q: 'Which stream operation is lazy?', options: ['collect()', 'forEach()', 'count()', 'filter()'], answer: 3, explanation: 'filter() is an intermediate (lazy) operation. collect(), forEach(), count() are terminal operations that trigger actual processing.' },
  { id: 2, q: 'What does flatMap() do differently from map()?', options: ['Filters elements', 'Transforms 1-to-many and flattens results', 'Applies transformation in parallel', 'Converts to a list'], answer: 1, explanation: 'flatMap() maps each element to a Stream and then flattens all those streams into one. map() is 1-to-1; flatMap() is 1-to-N then flatten.' },
  { id: 3, q: 'Which functional interface has method `boolean test(T t)`?', options: ['Function<T,R>', 'Consumer<T>', 'Predicate<T>', 'Supplier<T>'], answer: 2, explanation: 'Predicate<T> has boolean test(T t). Function<T,R> applies and returns R. Consumer<T> accepts and returns void. Supplier<T> gets with no input.' },
  { id: 4, q: 'What does orElseGet() return if Optional is present?', options: ['The default value', 'null', 'The Optional value (not the default)', 'Empty Optional'], answer: 2, explanation: 'If Optional is present, orElseGet returns the contained value. The supplier is only called if Optional is empty (lazy).' },
  { id: 5, q: 'Can you use a stream twice?', options: ['Yes, always', 'Yes, after calling reset()', 'No, streams are consumed after terminal operation', 'Yes, only for parallel streams'], answer: 2, explanation: 'Streams are single-use. After a terminal operation, the stream is consumed. Calling any operation on it throws IllegalStateException.' },
  { id: 6, q: 'What type does `Collectors.groupingBy(Person::getDept)` produce?', options: ['List<Person>', 'Map<String, List<Person>>', 'Set<String>', 'Optional<Person>'], answer: 1, explanation: 'groupingBy produces Map<K, List<T>> where K is the classifier result type and V is List of elements with that key.' },
  { id: 7, q: 'What is a method reference for `String::toUpperCase` called on stream elements?', options: ['Static method reference', 'Instance method on specific object', 'Instance method on arbitrary object of type', 'Constructor reference'], answer: 2, explanation: 'String::toUpperCase is an instance method reference on an arbitrary instance of type String — it calls toUpperCase() on each stream element.' },
  { id: 8, q: 'What does `Stream.generate(Math::random).limit(5)` produce?', options: ['A random number', '5 random numbers in a List', 'An infinite stream', 'A Stream of 5 random doubles'], answer: 3, explanation: 'Stream.generate creates an infinite stream. .limit(5) makes it finite — 5 random doubles. Still a Stream, not a List until collected.' },
  { id: 9, q: 'Which terminal operation short-circuits on first match?', options: ['collect()', 'reduce()', 'findFirst()', 'map()'], answer: 2, explanation: 'findFirst() (and findAny(), anyMatch(), noneMatch(), allMatch()) short-circuit — stop processing as soon as result is determined.' },
  { id: 10, q: 'What is the difference between orElse() and orElseGet()?', options: ['No difference', 'orElse always evaluates the default; orElseGet lazily evaluates it', 'orElseGet throws on empty; orElse does not', 'orElse returns Optional; orElseGet returns value'], answer: 1, explanation: 'orElse(val) always evaluates val. orElseGet(supplier) only calls supplier if Optional is empty — use for expensive computations.' },
  { id: 11, q: 'What happens when you call get() on an empty Optional?', options: ['Returns null', 'Returns default value', 'Throws NoSuchElementException', 'Returns Optional.empty()'], answer: 2, explanation: 'Optional.get() on empty throws NoSuchElementException. Always use orElse, orElseGet, or orElseThrow instead.' },
  { id: 12, q: 'What keyword allows a lambda to return from an enclosing switch expression?', options: ['return', 'break', 'yield', 'exit'], answer: 2, explanation: 'In a switch expression block-case, yield returns the value: `case 0 -> { yield compute(); }`. return exits the whole method.' },
  { id: 13, q: 'What does `IntStream.range(0, 5)` produce?', options: ['0 to 5 inclusive', '0 to 4 inclusive', '1 to 5 inclusive', '1 to 4 inclusive'], answer: 1, explanation: 'IntStream.range(0, 5) produces 0, 1, 2, 3, 4 (exclusive end). IntStream.rangeClosed(0, 5) produces 0-5 inclusive.' },
  { id: 14, q: 'What is CompletableFuture.thenApply() used for?', options: ['Runs after failure', 'Transforms the result when complete', 'Combines two futures', 'Provides default on error'], answer: 1, explanation: 'thenApply(fn) applies a Function to transform the result when the CompletableFuture completes successfully.' },
  { id: 15, q: 'Which Collector joins stream elements into a String with comma separator?', options: ['Collectors.toString(", ")', 'Collectors.joining(", ")', 'Collectors.concat(", ")', 'String.join()'], answer: 1, explanation: 'Collectors.joining(", ") collects Stream<String> elements separated by ", ". joining(delim, prefix, suffix) for wrapping.' },
]

export const shortNotes = {
  title: 'Functional Programming & Streams — Quick Reference',
  color: 'fuchsia',
  keyPoints: [
    { icon: '🔧', title: 'Key Functional Interfaces', text: 'Predicate<T> (test→bool), Function<T,R> (apply→R), Consumer<T> (accept→void), Supplier<T> (get→T), BiFunction.' },
    { icon: '🌊', title: 'Stream Pipeline', text: 'source → intermediate ops (lazy) → terminal op. Intermediate: filter, map, sorted, distinct, limit. Terminal triggers execution.' },
    { icon: '🔀', title: 'map vs flatMap', text: 'map: 1-to-1 transform. flatMap: 1-to-N + flatten. Use flatMap for nested collections or Optional chains.' },
    { icon: '📦', title: 'Collectors', text: 'toList(), toSet(), toMap(), joining(delim), groupingBy(classifier), partitioningBy(predicate), counting().' },
    { icon: '🎯', title: 'Optional', text: 'Never call get() directly. Use orElse, orElseGet (lazy!), orElseThrow, ifPresent, map, filter. Not for fields/params.' },
    { icon: '⚡', title: 'Method References', text: 'Static (Class::static), instance on specific (obj::method), instance on type (Class::instance), constructor (Class::new).' },
    { icon: '🔄', title: 'Parallel Streams', text: 'Use for CPU-bound, large data, no shared state. Not for I/O or small collections. Benchmark before using.' },
    { icon: '🚀', title: 'CompletableFuture', text: 'thenApply (transform), thenAccept (consume), thenCompose (chain), thenCombine (merge), exceptionally (error).' },
  ],
  quickFacts: [
    'Streams are lazy — nothing runs until terminal operation',
    'Streams are single-use — consumed after terminal op',
    'Stream.toList() (Java 16+) returns unmodifiable list',
    'flatMap and Optional.flatMap work the same way conceptually',
    'Primitive streams (IntStream) avoid boxing overhead',
    'orElseGet is lazy — supplier only called when Optional is empty',
    'CompletableFuture uses ForkJoinPool.commonPool() by default',
  ],
  rememberThis: [
    'Lambdas can only capture effectively final variables',
    'orElse() always evaluates the default — orElseGet() is lazy',
    'Never reuse a stream after terminal operation',
    'Parallel streams need no shared mutable state to be safe',
  ],
}
