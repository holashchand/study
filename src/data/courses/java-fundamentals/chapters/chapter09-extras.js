// Chapter 9: Concurrency — Top Questions, Quiz, Short Notes

export const topQuestions = [
  { id: 1, difficulty: 'medium', q: 'What is the difference between a process and a thread?', a: 'Process: independent program with its own memory space, heavy to create. Thread: execution unit within a process, shares heap memory. Java 1:1 maps platform threads to OS threads. Virtual threads (Java 21): lightweight JVM-managed threads, millions can coexist, mapped M:N to platform threads.' },
  { id: 2, difficulty: 'medium', q: 'What does `synchronized` do?', a: 'Ensures only one thread executes a synchronized block/method at a time (mutual exclusion) via monitor lock. synchronized method locks on `this` (instance) or Class (static). synchronized(obj) block uses explicit lock. Reentrant — same thread can re-acquire. Also provides visibility (flushes memory).' },
  { id: 3, difficulty: 'hard', q: 'What does `volatile` guarantee?', a: 'volatile: (1) writes immediately visible to all threads (happens-before guarantee), (2) prevents reordering around the variable. Does NOT make compound operations (i++) atomic. Use volatile for simple flags: `volatile boolean running = true;`. For atomics: use AtomicInteger, AtomicBoolean etc.' },
  { id: 4, difficulty: 'medium', q: 'What is a deadlock? How do you prevent it?', a: 'Deadlock: threads hold locks the other needs → circular wait. Prevention: (1) consistent lock ordering, (2) tryLock() with timeout, (3) avoid nested locks, (4) lock-free algorithms (CAS/atomics). Detection: thread dump (jstack). Deadlocks cause silent hangs — always design locking carefully.' },
  { id: 5, difficulty: 'medium', q: 'What is the difference between Runnable and Callable?', a: 'Runnable: `void run()` — no return, no checked exception. Callable<V>: `V call() throws Exception` — returns a value, can throw. Use Callable with ExecutorService.submit() which returns Future<V> for async result retrieval.' },
  { id: 6, difficulty: 'medium', q: 'What is ExecutorService and what thread pool types are available?', a: 'ExecutorService manages a pool of threads. Factory methods: Executors.newFixedThreadPool(n), newCachedThreadPool() (grows on demand), newSingleThreadExecutor(), newScheduledThreadPool(n). Always call shutdown() after use. Direct ThreadPoolExecutor for fine-grained control.' },
  { id: 7, difficulty: 'hard', q: 'What are virtual threads (Java 21)?', a: 'JVM-managed lightweight threads (Project Loom). Millions can exist simultaneously. Block without blocking OS threads — mount/unmount at blocking points. Created with Thread.ofVirtual().start() or Executors.newVirtualThreadPerTaskExecutor(). Best for I/O-bound workloads. Eliminates thread-per-request scalability limits.' },
  { id: 8, difficulty: 'hard', q: 'What is the Java Memory Model (JMM)?', a: 'JMM defines how threads interact with shared memory. Without synchronization, threads may see stale cached values. Happens-before relationship: guarantees visibility. Established by: synchronized blocks, volatile writes/reads, Thread.start/join, final fields of immutable objects. Without it: data races.' },
  { id: 9, difficulty: 'medium', q: 'What is the difference between wait(), notify(), notifyAll()?', a: 'Must be called inside synchronized block on the same object. wait(): releases lock and suspends. notify(): wakes one waiting thread. notifyAll(): wakes all. ALWAYS use wait() in a loop (while(!condition) wait()) for spurious wakeups. Prefer BlockingQueue or Condition for production code.' },
  { id: 10, difficulty: 'medium', q: 'What is ConcurrentHashMap vs synchronized Map?', a: 'ConcurrentHashMap: segment/node-level locking (Java 8+: CAS), allows concurrent reads and partial writes. No null keys/values. Weakly consistent iterators. synchronizedMap: entire map locked per operation — one thread at a time, much slower under contention. ConcurrentHashMap for production multi-threaded code.' },
  { id: 11, difficulty: 'hard', q: 'What is a ForkJoinPool?', a: 'Work-stealing ExecutorService for divide-and-conquer tasks. Idle threads steal from busy queues. Tasks extend RecursiveTask<V> (returns value) or RecursiveAction (no return). ForkJoinPool.commonPool() shared by parallel streams. Use fork()/join() for recursive parallelism.' },
  { id: 12, difficulty: 'medium', q: 'What is a BlockingQueue?', a: 'Thread-safe queue with blocking put (waits if full) and take (waits if empty). Implementations: ArrayBlockingQueue (bounded), LinkedBlockingQueue (bounded/unbounded), PriorityBlockingQueue (priority), SynchronousQueue (direct handoff). Perfect for producer-consumer patterns without explicit synchronization.' },
  { id: 13, difficulty: 'hard', q: 'What are atomic classes?', a: 'java.util.concurrent.atomic: AtomicInteger, AtomicLong, AtomicBoolean, AtomicReference, LongAdder (high-contention sum). Use CAS (Compare-And-Swap) CPU instructions — lock-free. `atomicInt.incrementAndGet()` is thread-safe without synchronized. LongAdder is faster than AtomicLong for frequent increments under contention.' },
  { id: 14, difficulty: 'medium', q: 'What is a ThreadLocal?', a: 'ThreadLocal provides per-thread storage — each thread has its own value. `ThreadLocal.withInitial(() -> new SimpleDateFormat())`. Common uses: per-thread db connections, request context in web frameworks. Warning: in thread pools, call remove() after use to prevent stale data. Virtual threads: ThreadLocal works but ScopedValue is preferred.' },
  { id: 15, difficulty: 'hard', q: 'What is a ReadWriteLock?', a: 'ReadWriteLock allows multiple concurrent readers but exclusive write access. `ReentrantReadWriteLock`: readLock() for shared reads, writeLock() for exclusive writes. Writers wait for all readers to release. Much better throughput than exclusive lock for read-heavy workloads. Upgrade from read to write lock not supported (must release read first).' },
  { id: 16, difficulty: 'medium', q: 'What is a CountDownLatch?', a: 'One-time synchronization barrier. Initialized with count. `await()` blocks until count reaches zero. `countDown()` decrements. Use for: wait for N services to start, wait for N tasks to complete. NOT reusable. For repeating barriers: use CyclicBarrier.' },
  { id: 17, difficulty: 'medium', q: 'What is a Semaphore?', a: 'Controls access to a limited number of resources. `new Semaphore(N)`: N permits. `acquire()` blocks if no permits available; `release()` returns a permit. Use for: connection pool limits, rate limiting, resource throttling. `tryAcquire()` for non-blocking attempt.' },
  { id: 18, difficulty: 'hard', q: 'What is structured concurrency (Java 21)?', a: 'StructuredTaskScope ensures subtasks don\'t outlive their scope. `try (var scope = new StructuredTaskScope.ShutdownOnFailure()) { scope.fork(task1); scope.fork(task2); scope.join(); }`. If one task fails, others are cancelled. Prevents thread leaks and simplifies error handling. Better than manual CompletableFuture chains.' },
  { id: 19, difficulty: 'medium', q: 'What is a race condition?', a: 'Two threads access shared data concurrently and at least one writes, with outcome depending on timing. Example: `if (count == max) count++;` — check-then-act is not atomic. Fix: use synchronized, volatile (for simple reads/writes), or atomics. Races are non-deterministic — hard to reproduce.' },
  { id: 20, difficulty: 'hard', q: 'What is the StampedLock in Java?', a: 'StampedLock (Java 8+): faster than ReadWriteLock, supports optimistic reads. Methods: writeLock(), readLock(), tryOptimisticRead() + validate(stamp). Optimistic read: reads without lock, validates afterward — if invalidated, retry with regular lock. Not reentrant — cannot be used recursively. Best for very high-performance read scenarios.' },
]

export const quiz = [
  { id: 1, q: 'What does synchronized guarantee?', options: ['Visibility only', 'Atomicity only', 'Mutual exclusion + memory visibility', 'Only prevents deadlocks'], answer: 2, explanation: 'synchronized provides: (1) mutual exclusion — one thread at a time, (2) memory visibility — flushes thread-local caches to main memory.' },
  { id: 2, q: 'What does volatile NOT guarantee?', options: ['Memory visibility', 'Happens-before ordering', 'Atomicity of compound operations (i++)', 'Prevents reordering'], answer: 2, explanation: 'volatile guarantees visibility and ordering but NOT atomicity. i++ is read-modify-write — not atomic. Use AtomicInteger for that.' },
  { id: 3, q: 'Which is the recommended way to create a thread pool?', options: ['new Thread() directly', 'Executors.newFixedThreadPool(n)', 'synchronized block', 'ThreadGroup'], answer: 1, explanation: 'ExecutorService (via Executors factory) manages thread lifecycle. Direct Thread creation is error-prone and unmanaged.' },
  { id: 4, q: 'What is a deadlock?', options: ['A thread running too slowly', 'Two threads waiting for each other\'s lock indefinitely', 'A thread that throws an exception', 'Memory corruption'], answer: 1, explanation: 'Deadlock: Thread A holds Lock1 and waits for Lock2; Thread B holds Lock2 and waits for Lock1 → neither can proceed.' },
  { id: 5, q: 'What Java version introduced virtual threads as a stable feature?', options: ['Java 17', 'Java 19', 'Java 21', 'Java 23'], answer: 2, explanation: 'Virtual threads (Project Loom) became stable in Java 21 LTS (JEP 444).' },
  { id: 6, q: 'Which is thread-safe without external synchronization?', options: ['ArrayList', 'HashMap', 'StringBuilder', 'AtomicInteger'], answer: 3, explanation: 'AtomicInteger uses CAS (Compare-And-Swap) CPU instructions for lock-free thread-safety. ArrayList, HashMap, StringBuilder are NOT thread-safe.' },
  { id: 7, q: 'What must be true when using wait()/notify()?', options: ['Thread must hold the lock on the object', 'Thread must be in a static context', 'Thread must be the main thread', 'Thread must call sleep() first'], answer: 0, explanation: 'wait() and notify() must be called inside a synchronized block on the same object (the monitor). Otherwise: IllegalMonitorStateException.' },
  { id: 8, q: 'What does CountDownLatch do?', options: ['Limits thread count', 'Blocks until count reaches 0', 'Creates threads automatically', 'Prevents deadlocks'], answer: 1, explanation: 'CountDownLatch: await() blocks until countDown() is called enough times to reach 0. One-time use — not reusable.' },
  { id: 9, q: 'What is the difference between Future and CompletableFuture?', options: ['No difference', 'Future: can compose async pipelines; CompletableFuture: cannot', 'CompletableFuture: composable (thenApply, etc.); Future: can only get() blocking', 'Future is Java 8+; CompletableFuture is Java 5+'], answer: 2, explanation: 'CompletableFuture extends Future with composable async operations (thenApply, thenCompose, etc.). Future only has blocking get().' },
  { id: 10, q: 'Which collection type is designed for producer-consumer patterns?', options: ['ArrayList', 'HashMap', 'BlockingQueue', 'PriorityQueue'], answer: 2, explanation: 'BlockingQueue blocks put() when full and take() when empty — naturally suited for producer-consumer without explicit synchronization.' },
  { id: 11, q: 'What happens if you don\'t remove a ThreadLocal value in a thread pool?', options: ['Nothing, it is automatically cleared', 'Memory leak — next task gets previous task\'s value', 'The thread crashes', 'The value becomes null'], answer: 1, explanation: 'Thread pool threads are reused. If ThreadLocal is not removed, the next task on that thread gets the stale value. Always call remove() after use.' },
  { id: 12, q: 'What is work-stealing in ForkJoinPool?', options: ['Threads steal CPU time from others', 'Idle threads take tasks from busy threads\' queues', 'Threads share a single work queue', 'Tasks are randomly assigned'], answer: 1, explanation: 'Work-stealing: each thread has a deque. Idle threads pop from the tail of busy threads\' deques. Balances load efficiently.' },
  { id: 13, q: 'Which lock allows multiple concurrent readers?', options: ['ReentrantLock', 'synchronized', 'ReadWriteLock (readLock)', 'StampedLock (writeLock)'], answer: 2, explanation: 'ReadWriteLock.readLock() allows multiple threads to read simultaneously. writeLock() is exclusive. Good for read-heavy workloads.' },
  { id: 14, q: 'What is a race condition?', options: ['Thread running faster than expected', 'Outcome depends on non-deterministic thread timing', 'A synchronized block timing out', 'Thread pool running out of threads'], answer: 1, explanation: 'Race condition: shared data accessed by multiple threads with at least one writer, and the result depends on execution timing.' },
  { id: 15, q: 'What is the best concurrency tool for I/O-bound workloads in Java 21?', options: ['Fixed thread pool', 'Parallel streams', 'Virtual threads', 'ForkJoinPool'], answer: 2, explanation: 'Virtual threads (Java 21) are designed for I/O-bound work — they block without consuming OS threads, enabling millions of concurrent I/O operations.' },
]

export const shortNotes = {
  title: 'Concurrency — Quick Reference',
  color: 'rose',
  keyPoints: [
    { icon: '🧵', title: 'Threads', text: 'Platform threads: 1:1 OS threads. Virtual threads (Java 21): lightweight JVM-managed, millions can coexist, ideal for I/O.' },
    { icon: '🔐', title: 'synchronized', text: 'Mutual exclusion + memory visibility. Reentrant. synchronized method → this (instance) or Class (static).' },
    { icon: '⚡', title: 'volatile', text: 'Visibility + ordering. NOT atomicity. Use for flags. Use AtomicInteger for read-modify-write operations.' },
    { icon: '💀', title: 'Deadlock Prevention', text: 'Consistent lock ordering, tryLock with timeout, minimize nested locks, prefer lock-free algorithms.' },
    { icon: '🏊', title: 'Thread Pools', text: 'Executors.newFixedThreadPool(n), newCachedThreadPool(), newVirtualThreadPerTaskExecutor() (Java 21). Always shutdown().' },
    { icon: '🔄', title: 'Blocking Queue', text: 'Thread-safe, blocking put/take. Perfect for producer-consumer. ArrayBlockingQueue, LinkedBlockingQueue.' },
    { icon: '⚛️', title: 'Atomic Classes', text: 'Lock-free CAS operations. AtomicInteger, AtomicReference, LongAdder (high contention). incrementAndGet() thread-safe.' },
    { icon: '🏗️', title: 'Structured Concurrency', text: 'Java 21: StructuredTaskScope. Tasks don\'t outlive scope. Cancels siblings on failure. Prevents thread leaks.' },
  ],
  quickFacts: [
    'Virtual threads became stable in Java 21 (JEP 444)',
    'CountDownLatch is one-time use; CyclicBarrier is reusable',
    'ThreadLocal in thread pools: always call remove() after use',
    'ConcurrentHashMap does not allow null keys/values',
    'synchronized is reentrant — same thread can re-acquire',
    'ForkJoinPool.commonPool() is shared by parallel streams',
    'LongAdder outperforms AtomicLong under high contention',
  ],
  rememberThis: [
    'volatile ensures visibility, NOT atomicity (i++ is not safe with volatile)',
    'wait() must be in a while loop to handle spurious wakeups',
    'synchronized method on static → locks the Class object, not instance',
    'Thread.start() starts new thread; Thread.run() runs in current thread',
  ],
}
