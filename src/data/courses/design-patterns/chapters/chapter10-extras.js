// Chapter 10: Architectural Patterns (MVC, MVP, MVVM, Repository, Service Locator)

export const topQuestions = [
  { id: 1, difficulty: 'easy', q: 'What is the MVC pattern?', a: 'MVC (Model-View-Controller) separates an application into three layers: Model (data/logic), View (UI/presentation), Controller (input handling). MVC decouples UI from business logic, making apps easier to test and maintain.' },
  { id: 2, difficulty: 'easy', q: 'What is the Repository pattern?', a: 'The Repository pattern provides an abstraction layer over data access. It encapsulates data retrieval/persistence logic, making code independent of the data source. Clients interact with repositories instead of directly accessing databases.' },
  { id: 3, difficulty: 'easy', q: 'How does MVC separate concerns?', a: 'Model holds data and logic (independent of UI). View renders data (no logic). Controller handles input and orchestrates Model/View. Each layer has a single responsibility: data, presentation, or coordination.' },
  { id: 4, difficulty: 'easy', q: 'What is the difference between MVC and MVP?', a: 'MVC: View updates when Model changes (observer). MVP: Presenter entirely controls View; View is passive. MVP enables easier testing because the Presenter is independent of the UI framework.' },
  { id: 5, difficulty: 'easy', q: 'What is the MVVM pattern?', a: 'MVVM (Model-View-ViewModel) separates concerns with: Model (data/logic), View (UI), ViewModel (binds View to Model). ViewModel exposes data via properties; View binds to these properties and auto-updates when data changes (two-way binding).' },
  { id: 6, difficulty: 'medium', q: 'What is the key difference between MVC Controller and MVP Presenter?', a: 'Controller: handles input, updates Model, View observes Model changes. Presenter: handles all View interactions, updates View directly, updates Model. MVP Presenter controls the entire View; MVC Controller is more loosely coupled.' },
  { id: 7, difficulty: 'medium', q: 'How does MVVM data binding work?', a: 'ViewModel exposes observable properties. View binds to these properties declaratively (e.g., `v-model` in Vue, `binding` in WPF). When data changes, the binding automatically updates the UI. Two-way binding: UI changes update the ViewModel too.' },
  { id: 8, difficulty: 'medium', q: 'What does the Repository pattern abstract?', a: 'Repository abstracts the data access layer. It hides: (1) data source location (database, cache, API), (2) query language/implementation, (3) persistence logic. Clients use a consistent interface regardless of where data actually comes from.' },
  { id: 9, difficulty: 'medium', q: 'Why is Service Locator considered an anti-pattern?', a: 'Service Locator hides dependencies — classes don\'t declare what they need, they ask a service locator for dependencies. This makes code harder to test (hard to mock), harder to understand (dependencies are implicit), and creates tight coupling to the locator.' },
  { id: 10, difficulty: 'medium', q: 'What are the layers in layered (N-tier) architecture?', a: 'Common layers (bottom to top): Persistence (database, queries), Business Logic (domain rules), Application/Service (orchestration, transactions), Presentation (UI/API). Each layer depends only on layers below, not above — the Dependency Rule.' },
  { id: 11, difficulty: 'medium', q: 'What is the difference between Repository and DAO (Data Access Object)?', a: 'DAO is lower-level — closer to the database (SQL queries). Repository is higher-level — works with domain objects and collections. DAO is database-specific; Repository is business logic-focused. Repository often uses multiple DAOs internally.' },
  { id: 12, difficulty: 'medium', q: 'How does Spring Data JPA implement the Repository pattern?', a: 'Spring Data JPA provides Repository<T, ID> interface with CRUD methods (save, findById, delete). You define custom queries with @Query or method naming conventions. Spring generates implementations automatically, abstracting Hibernate/JPA details.' },
  { id: 13, difficulty: 'medium', q: 'What is the difference between passive View and active View in MVC?', a: 'Active View: observes Model changes, updates itself automatically. Passive View: does NOT update itself — Controller updates View after Model changes. Passive View (MVP style) is easier to test; Active View (traditional MVC) reduces Controller complexity.' },
  { id: 14, difficulty: 'medium', q: 'Why is MVP preferred over MVC for unit testing?', a: 'MVP: Presenter has no UI dependencies — easy to unit test with mock Views. MVC: Controller needs a View to test — either you need UI framework in tests, or you decouple View (essentially creating MVP). MVP makes testability inherent.' },
  { id: 15, difficulty: 'medium', q: 'How do Angular, Vue, and WPF use MVVM?', a: 'Angular: Component is ViewModel, Template is View. Vue: data() is ViewModel, template is View. WPF: ViewModel is a .NET class, View is XAML. All support two-way binding between View and ViewModel. Frameworks abstract binding mechanics.' },
  { id: 16, difficulty: 'hard', q: 'How do you test MVC without a web framework?', a: 'Extract Controller logic into a testable service/use case class. Test service logic (pure functions, no web). Test Controller with a mock View/HttpRequest. For View, use browser automation (Selenium) or snapshot testing. Example: Java, test controller methods with MockMvc or manual mocking.' },
  { id: 17, difficulty: 'hard', q: 'What is the relationship between Repository and Unit of Work pattern?', a: 'Unit of Work manages a transaction\'s changes to multiple entities. Repository handles single-entity persistence. Together: Unit of Work wraps multiple repositories\' changes, commits them atomically. Example: save Customer and Order in one transaction via UnitOfWork.' },
  { id: 18, difficulty: 'hard', q: 'How does CQRS relate to the Repository pattern?', a: 'CQRS (Command Query Responsibility Segregation) separates reads and writes. Repository can implement CQRS: write repository handles commands (insert/update/delete), read repository handles queries (optimized reads, denormalized views). Enables scaling reads and writes independently.' },
  { id: 19, difficulty: 'hard', q: 'How does layered architecture enforce the Dependency Rule?', a: 'Dependency Rule: dependencies point inward (high-level layers depend on low-level). Presentation → Application → Business → Persistence. Violations: Presentation directly accessing database (circular, tight coupling). Enforce via: packages, imports restrictions, architecture tests.' },
  { id: 20, difficulty: 'hard', q: 'For a Java desktop app, which is better: MVC, MVP, or MVVM? Why?', a: 'MVP or MVVM: Desktop apps benefit from testable presenters/viewmodels. MVVM if framework supports binding (e.g., JavaFX with ControlsFX). MVP if binding is manual (Swing). Avoid pure MVC — its active View model is less testable. Desktop isolation allows easier automation than web testing.' },
]

export const quiz = [
  { id: 1, q: 'What does MVC stand for?', options: ['Model-View-Component', 'Model-View-Controller', 'Multi-View-Container', 'Model-Validation-Controller'], answer: 1, explanation: 'MVC = Model-View-Controller. Model holds data, View presents it, Controller handles input.' },
  { id: 2, q: 'In MVC, what is the primary responsibility of the Model?', options: ['Display data to user', 'Handle user input', 'Store data and contain business logic', 'Manage the view hierarchy'], answer: 2, explanation: 'Model encapsulates data and business rules. It should be independent of UI (can be tested without a View).' },
  { id: 3, q: 'What is the key characteristic of a Passive View in MVP?', options: ['It observes Model changes automatically', 'It has no UI logic and is entirely controlled by Presenter', 'It manages its own updates', 'It directly accesses the Model'], answer: 1, explanation: 'Passive View: dumb, no logic. Presenter controls it entirely, updates it after handling Model changes or user input.' },
  { id: 4, q: 'How does MVVM data binding work in Vue.js?', options: ['v-model creates two-way binding between View and ViewModel', 'Manually calling update() in methods', 'Using explicit event listeners everywhere', 'Direct DOM manipulation'], answer: 0, explanation: 'v-model binds input values to ViewModel properties. Changes propagate automatically both ways.' },
  { id: 5, q: 'What does Repository pattern abstract?', options: ['The View layer', 'The database and data access logic', 'The business logic', 'The Controller'], answer: 1, explanation: 'Repository provides a collection-like interface to data, hiding where/how it\'s stored. Enables swapping data sources.' },
  { id: 6, q: 'Why is Service Locator considered an anti-pattern?', options: ['It\'s too slow', 'Dependencies are implicit; hard to test and understand', 'It uses reflection', 'It doesn\'t work with dependency injection'], answer: 1, explanation: 'Service Locator hides dependencies. Classes don\'t declare needs — they ask the locator. Makes testing harder (can\'t inject mocks easily) and code harder to follow.' },
  { id: 7, q: 'In layered architecture, which direction do dependencies point?', options: ['Outward (low to high)', 'Inward (high to low)', 'Circular', 'They don\'t have dependencies'], answer: 1, explanation: 'Dependency Rule: dependencies point inward. Presentation → Application → Business → Persistence. High-level layers depend on low-level, not vice versa.' },
  { id: 8, q: 'What is the difference between Repository and DAO?', options: ['No difference; terms are synonymous', 'DAO is higher-level; Repository is lower-level', 'Repository is higher-level collection-like interface; DAO is lower-level database-specific', 'Repository works with SQL; DAO works with objects'], answer: 2, explanation: 'DAO (Data Access Object): low-level, database-specific, returns raw data. Repository: high-level, works with domain objects, business-logic focused.' },
  { id: 9, q: 'How does Spring Data JPA implement Repository pattern?', options: ['Developers write all CRUD code manually', 'Interface extends Repository<T, ID>; Spring generates implementations via proxies', 'Using Service Locator', 'Direct JDBC queries'], answer: 1, explanation: 'Spring Data JPA: extend Repository<T, ID> (or CrudRepository). Spring generates implementations for standard CRUD + custom queries via method naming or @Query.' },
  { id: 10, q: 'In MVC, when does the View update after user input?', options: ['Immediately when user interacts', 'When Controller updates Model, then View observes and updates', 'Manually by the View itself', 'Only if an Observer is attached'], answer: 1, explanation: 'MVC flow: User → Controller → updates Model → View observes and updates. View is reactive to Model changes.' },
  { id: 11, q: 'Why is MVP easier to unit test than MVC?', options: ['It doesn\'t require testing', 'Presenter is UI-framework independent; mock the View and test logic', 'MVP runs faster', 'They\'re equally testable'], answer: 1, explanation: 'MVP: Presenter is a pure class (no UI dependencies). Test with mock View. MVC: Controller often tightly coupled to View/HttpRequest (harder to unit test).' },
  { id: 12, q: 'What does the ViewModel expose in MVVM?', options: ['Only data', 'Data AND commands/methods that View binds to', 'Nothing; it just processes data privately', 'Only UI state'], answer: 1, explanation: 'ViewModel exposes observable properties and methods. View binds to these. Changes propagate automatically via data binding.' },
  { id: 13, q: 'Which architectural layer should contain business rules?', options: ['Presentation', 'Business Logic / Domain', 'Persistence', 'All layers equally'], answer: 1, explanation: 'Business Logic layer (or Domain) contains domain rules, workflows, validations. Should be independent of Presentation and Persistence.' },
  { id: 14, q: 'What is the Unit of Work pattern relationship to Repository?', options: ['They conflict', 'Unit of Work wraps multiple Repositories in a transaction; commits atomically', 'Repository IS Unit of Work', 'No relationship'], answer: 1, explanation: 'Unit of Work manages changes to multiple entities across transaction boundaries. Repository handles single entities. Together: Unit of Work coordinates multiple repositories.' },
  { id: 15, q: 'In CQRS, how does Repository pattern apply?', options: ['Repository handles both reads and writes equally', 'Separate write repository (commands) and read repository (queries); optimize each independently', 'CQRS doesn\'t use Repository', 'Repository only handles queries'], answer: 1, explanation: 'CQRS + Repository: separate repositories for commands (normalized writes) and queries (denormalized reads). Enables independent scaling.' },
]

export const shortNotes = {
  title: 'Architectural Patterns — MVC, MVP, MVVM, Repository, Layered',
  color: 'pink',
  keyPoints: [
    { icon: '🏗️', title: 'MVC Pattern', text: 'Model (data/logic) — View (render) — Controller (input). View observes Model changes. Good separation, but View can become complex.' },
    { icon: '📊', title: 'MVP Pattern', text: 'Model — Passive View (no logic) — Presenter (controls View). Presenter orchestrates everything. View is testable; Presenter is pure logic.' },
    { icon: '🔄', title: 'MVVM Pattern', text: 'Model — ViewModel (observable properties) — View (binds to VM). Two-way binding auto-updates. Ideal for frameworks with binding support (Vue, WPF, Angular).' },
    { icon: '💾', title: 'Repository Pattern', text: 'Abstracts data access behind a collection-like interface. Hide database/SQL. Client code is data-source agnostic. Enables testing with in-memory repos.' },
    { icon: '📚', title: 'Layered Architecture', text: 'Presentation → Application → Business → Persistence. Dependencies point inward (high depends on low). Each layer has one responsibility.' },
    { icon: '🔗', title: 'Dependency Rule', text: 'Core principle: high-level layers depend on low-level, NEVER vice versa. Violations create circular dependencies and tight coupling.' },
    { icon: '⚠️', title: 'Service Locator Anti-Pattern', text: 'Classes ask locator for dependencies instead of declaring them. Implicit deps, hard to test, hard to understand. Use Dependency Injection instead.' },
    { icon: '🎯', title: 'Repository vs DAO', text: 'DAO: low-level, database-specific. Repository: high-level, works with domain objects. Repository may use multiple DAOs internally.' },
  ],
  quickFacts: [
    'MVC separates concerns but View can become a "God Object"',
    'MVP makes testability inherent by isolating Presenter from UI',
    'MVVM is perfect for reactive frameworks (Vue, Angular, React)',
    'Repository enables swapping databases without changing business logic',
    'Layered architecture is the most practical for enterprise apps',
    'Service Locator is an anti-pattern — prefer Dependency Injection (constructor, setter)',
    'Spring Data JPA makes Repository pattern easy with auto-generated queries',
    'Dependency Rule: never let high-level code depend on low-level implementation',
  ],
  rememberThis: [
    'MVC: Controller orchestrates. View observes Model. (Traditional but less testable)',
    'MVP: Presenter controls everything. View is passive. (More testable, clear dependencies)',
    'MVVM: Binding auto-syncs View and ViewModel. (Best for reactive UI frameworks)',
    'Repository: abstract data access. Unit of Work: coordinate multiple repos in transactions.',
  ],
}
